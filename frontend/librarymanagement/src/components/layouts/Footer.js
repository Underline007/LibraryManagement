import React from 'react';
import { Layout } from 'antd';

const { Footer } = Layout;

const AppFooter = () => {
    return (
        <Footer style={{ textAlign: 'center' }}>
            Library Management - NashTech
        </Footer>
    );
};

export default AppFooter;
